# 🚀 Быстрый старт Temporal OMS Demo

## 📋 Предварительные требования

### 1. Установите необходимое ПО:
- **Node.js** версии 18+ (рекомендуется 20+)
- **Docker** и **Docker Compose**
- **Git** (для клонирования репозитория)

### 2. Проверьте установку:
```bash
node --version
docker --version
```

## 🎯 Структура проекта

```
SA_docs/temporal/
├── docker-compose.yml          # Docker конфигурация
├── package.json               # Зависимости проекта
├── src/                       # Исходный код
│   ├── workflows/             # Workflow определения
│   │   └── order-workflow.ts  # Основной workflow заказа
│   ├── activities/            # Activity функции
│   │   └── order-activities.ts # Бизнес-операции
│   ├── client/                # Клиент для запуска
│   │   └── order-client.ts    # Демо-клиент
│   └── worker/                # Worker для выполнения
│       └── order-worker.ts    # Worker процесс
├── dynamicconfig/             # Конфигурация Temporal
└── README.md                  # Документация
```

## 🚀 Пошаговый запуск демо

### Шаг 1: Перейдите в папку проекта
```bash
cd "C:\Users\user\Desktop\GIT\SA_docs\temporal"
```

### Шаг 2: Установите зависимости npm
```bash
npm install
```

### Шаг 3: Запустите Temporal сервер через Docker
```bash
docker-compose up -d
```

**Проверьте статус контейнеров:**
```bash
docker-compose ps
```

**Должны быть активны:**
- `temporal-temporal-1` (порт 7233) - основной сервер
- `temporal-temporal-ui-1` (порт 8080) - веб-интерфейс
- `temporal-postgresql-1` (порт 5432) - база данных

### Шаг 4: Откройте Temporal UI
```bash
start http://localhost:8080
```

Или откройте в браузере: http://localhost:8080

### Шаг 5: Запустите Worker (в отдельном терминале)
```bash
npm run worker
```

**Ожидаемый вывод:**
```
✅ Worker создан и готов к работе
📋 Зарегистрированные workflow: orderWorkflow
🔧 Зарегистрированные activities: [...]
⏳ Ожидание задач в очереди: order-processing...
```

### Шаг 6: Запустите Client для демонстрации (в новом терминале)
```bash
npm run client
```

## 🎮 Что происходит в демо

### Демо 1: Успешная обработка заказа
1. **Создание заказа** - клиент создает новый заказ
2. **Проверка запасов** - проверка наличия товаров на складе
3. **Обработка платежа** - валидация платежной информации
4. **Подготовка к отправке** - сборка и упаковка заказа
5. **Отправка заказа** - передача в службу доставки
6. **Уведомление клиента** - отправка статуса заказа

### Демо 2: Отмена заказа
- Демонстрация работы **сигналов** Temporal
- Отмена workflow в процессе выполнения
- Обработка отмены и возврат средств

## 🔧 Activity функции

- `checkInventory()` - проверка наличия товаров
- `processPayment()` - обработка платежа
- `prepareOrderForShipment()` - подготовка к отправке
- `shipOrder()` - отправка заказа
- `notifyCustomer()` - уведомление клиента
- `cancelOrder()` - отмена заказа

## 📊 Мониторинг

### Temporal UI (http://localhost:8080)
- **Workflows** - просмотр всех запущенных workflow
- **Task Queues** - мониторинг очередей задач
- **History** - детальная история выполнения workflow

### Логи
- Worker логирует все операции в консоль
- Activity функции также логируют свои действия

## 🛠️ Полезные команды

### Проверка статуса контейнеров
```bash
docker-compose ps
```

### Просмотр логов
```bash
docker-compose logs temporal
docker-compose logs temporal-ui
```

### Перезапуск сервисов
```bash
docker-compose restart
```

### Остановка всех сервисов
```bash
docker-compose down
```

## ❗ Устранение проблем

### Ошибка подключения к Temporal
```bash
# Проверьте, что сервер запущен
docker-compose ps

# Перезапустите сервер если нужно
docker-compose restart temporal
```

### Ошибки TypeScript
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### Проблемы с портами
Если порты заняты, измените их в `docker-compose.yml`:
```yaml
ports:
  - "7234:7233"  # вместо 7233
  - "8081:8080"  # вместо 8080
```

## 📚 Концепции Temporal в демо

- **Workflow** - долгоживущий процесс обработки заказа
- **Activity** - отдельные бизнес-операции
- **Сигналы** - взаимодействие с запущенным workflow
- **Надежность** - продолжение после сбоев
- **Отслеживаемость** - полная история выполнения

## 🎯 Этапы Workflow заказа

1. **CREATED** - заказ создан
2. **INVENTORY_CHECKED** - запасы проверены
3. **PAYMENT_PROCESSED** - платеж обработан
4. **PREPARING_FOR_SHIPMENT** - подготовка к отправке
5. **SHIPPED** - заказ отправлен
6. **DELIVERED** - заказ доставлен

## 💡 Для дальнейшего изучения

1. **Изучите код** в папке `src/` для понимания реализации
2. **Попробуйте изменить** логику activity функций
3. **Добавьте новые этапы** в workflow
4. **Изучите Temporal UI** для мониторинга выполнения

## 🎮 Live Demo - Визуализация в реальном времени

### Запуск интерактивного демо:
```bash
# В новом терминале запустите live сервер
npm run live-demo

# Откройте веб-интерфейс
start http://localhost:3000
```

### Возможности Live Demo:
- **Визуализация workflow** в реальном времени
- **Логи выполнения** с цветовой кодировкой
- **Управление workflow** через веб-интерфейс
- **Статистика выполнения** и прогресс-бар

Подробнее в файле [`LIVE_DEMO.md`](LIVE_DEMO.md)

## 🔗 Полезные ссылки

- [Temporal Documentation](https://docs.temporal.io/)
- [Temporal TypeScript SDK](https://typescript.temporal.io/)
- [Temporal UI Guide](https://docs.temporal.io/ui)

---

**Демо готово к использованию! Начните изучение Temporal с запуска Worker и Client в отдельных терминалах или используйте Live Demo для интерактивной визуализации.**