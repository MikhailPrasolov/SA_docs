# Установка и запуск Temporal Demo OMS

## 🎯 Где выполнять команды?

**Все команды выполняются в командной строке (PowerShell или Command Prompt) в папке проекта:**

```bash
# Перейдите в папку проекта
cd "C:\Users\user\Desktop\GIT\SA_docs\temporal"
```

## 🚀 Быстрый старт (рекомендуется для начала)

### Запуск упрощенного демо без установки:

```bash
# В командной строке выполните:
powershell -ExecutionPolicy Bypass -File "powershell-demo.ps1"
```

### Запуск визуального демо в браузере:

```bash
# В командной строке выполните:
start visual-demo.html
```

## Полная установка (для продвинутого изучения)

## Предварительные требования

1. **Node.js** версии 18 или выше
2. **Docker** и **Docker Compose**
3. **Git** (для клонирования репозитория)

## Установка

### 1. Установка зависимостей

```bash
# Перейдите в папку проекта
cd SA_docs/temporal

# Установите зависимости npm
npm install
```

### 2. Запуск Temporal сервера

Temporal требует запущенный сервер для работы. Используем Docker Compose:

```bash
# Запуск Temporal сервера и UI
docker-compose up -d
```

Проверьте, что сервисы запущены:
```bash
docker-compose ps
```

Должны быть активны:
- `temporal` (порт 7233) - основной сервер
- `temporal-ui` (порт 8080) - веб-интерфейс
- `postgresql` (порт 5432) - база данных

### 3. Проверка установки

Откройте Temporal UI в браузере: http://localhost:8080

Вы должны увидеть веб-интерфейс Temporal с пустыми списками workflow.

## Запуск демо

### Запуск Worker

Worker выполняет workflow и activity. Запустите его в отдельном терминале:

```bash
npm run worker
```

Вы должны увидеть сообщение:
```
🚀 Запуск Temporal Worker для системы OMS...
✅ Worker создан и готов к работе
📋 Зарегистрированные workflow: orderWorkflow
🔧 Зарегистрированные activities: checkInventory,processPayment,prepareOrderForShipment,shipOrder,notifyCustomer,cancelOrder
⏳ Ожидание задач в очереди: order-processing...
```

### Запуск Client

В другом терминале запустите клиент для демонстрации:

```bash
npm run client
```

Клиент запустит демо-сценарий:
1. Успешная обработка заказа
2. Отмена заказа в процессе выполнения

## Структура демо

### Workflow: `orderWorkflow`
- Долгоживущий процесс обработки заказа
- Состояния: CREATED → INVENTORY_CHECKED → PAYMENT_PROCESSED → PREPARING_FOR_SHIPMENT → SHIPPED → DELIVERED

### Activity функции:
- `checkInventory` - проверка наличия товаров
- `processPayment` - обработка платежа
- `prepareOrderForShipment` - подготовка к отправке
- `shipOrder` - отправка заказа
- `notifyCustomer` - уведомление клиента
- `cancelOrder` - отмена заказа

### Сигналы:
- `cancelOrderSignal` - отмена workflow по запросу клиента

## Мониторинг

### Temporal UI
- **Workflows**: Просмотр всех запущенных workflow
- **Task Queues**: Мониторинг очередей задач
- **History**: Детальная история выполнения workflow

### Логи
- Worker логирует все операции в консоль
- Activity функции также логируют свои действия

## Устранение проблем

### Ошибка подключения к Temporal
```bash
# Проверьте, что сервер запущен
docker-compose ps

# Перезапустите сервер если нужно
docker-compose restart temporal
```

### Ошибки TypeScript
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install

# Проверьте типы
npx tsc --noEmit
```

### Проблемы с портами
Если порты заняты, измените их в `docker-compose.yml`:
```yaml
ports:
  - "7234:7233"  # вместо 7233
  - "8081:8080"  # вместо 8080
```

## Дальнейшие шаги

1. Изучите код в папке `src/` чтобы понять как работают workflow и activity
2. Попробуйте изменить логику activity функций
3. Добавьте новые этапы в workflow
4. Изучите Temporal UI для мониторинга выполнения

## Полезные ссылки

- [Temporal Documentation](https://docs.temporal.io/)
- [Temporal TypeScript SDK](https://typescript.temporal.io/)
- [Temporal UI Guide](https://docs.temporal.io/ui)