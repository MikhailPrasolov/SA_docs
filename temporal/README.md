# Temporal Demo - Система управления заказами (OMS)

Это демо-проект для изучения Temporal на примере системы управления заказами (Order Management System).

## 🎯 Особенности проекта

- **Полная реализация** workflow обработки заказа
- **Live Demo** с веб-интерфейсом для визуализации
- **Множество вариантов запуска** (PowerShell, браузер, Node.js)
- **Подробная документация** по концепциям Temporal
- **Готов к использованию** после исправления ошибок

## Структура проекта

```
temporal/
├── README.md                 # Основная документация проекта
├── package.json              # Зависимости проекта
├── docker-compose.yml        # Docker для Temporal сервера
├── src/
│   ├── workflows/            # Workflow определения
│   │   └── order-workflow.ts # Workflow процесса заказа
│   ├── activities/           # Activity функции
│   │   └── order-activities.ts # Бизнес-процессы заказа
│   ├── client/               # Клиент для запуска workflow
│   │   └── order-client.ts   # Клиент заказа
│   ├── worker/               # Worker для выполнения workflow
│   │   └── order-worker.ts   # Worker заказа
│   └── types/                # TypeScript типы
│       └── order.ts          # Типы данных заказа
├── live-server.js            # Live Demo сервер
├── live-demo.html            # Веб-интерфейс Live Demo
├── browser-demo.html         # Браузерная версия демо
├── powershell-demo.ps1       # PowerShell демо
└── simple-demo.js            # Упрощенная Node.js версия
```

## Описание демо

Демо показывает процесс обработки заказа в системе OMS с использованием Temporal:

1. **Создание заказа** - клиент создает новый заказ
2. **Проверка запасов** - проверка наличия товаров на складе
3. **Проверка платежа** - валидация платежной информации
4. **Подготовка к отправке** - сборка и упаковка заказа
5. **Отправка заказа** - передача в службу доставки
6. **Уведомление клиента** - отправка статуса заказа

## 🚀 Быстрый запуск

### Вариант 1: Live Demo (рекомендуется)
```bash
# Запустите Live Demo сервер
npm run live-demo

# Откройте в браузере: http://localhost:3000
```

### Вариант 2: Полная версия с Temporal сервером
```bash
# Установите зависимости
npm install

# Запустите Temporal сервер (требует Docker)
docker-compose up -d

# Запустите worker (в отдельном терминале)
npm run worker

# Запустите клиент (в новом терминале)
npm run client
```

### Вариант 3: Упрощенные версии
- **PowerShell**: `powershell -ExecutionPolicy Bypass -File "powershell-demo.ps1"`
- **Браузер**: откройте `browser-demo.html`
- **Node.js**: `node simple-demo.js`

## 🛠️ Исправленные проблемы

- ✅ **Исправлена ошибка** `toLocaleDateString is not a function` в workflow
- ✅ **Обновлена документация** в соответствии с текущим состоянием
- ✅ **Улучшена структура проекта** и инструкции по запуску

## ❗ Важные замечания

- **Temporal сервер должен быть запущен** перед запуском worker и client
- **Worker и client должны запускаться в разных терминалах**
- **Live Demo требует запущенного worker** для работы

## 📚 Temporal концепции

- **Workflow**: Долгоживущий процесс обработки заказа
- **Activity**: Отдельные бизнес-операции (проверка запасов, платежа и т.д.)
- **Worker**: Процесс, который выполняет workflow и activity
- **Client**: Запускает workflow и запрашивает статус
- **Сигналы**: Взаимодействие с запущенным workflow (отмена заказа)