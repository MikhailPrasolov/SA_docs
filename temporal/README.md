# Temporal Demo - Система управления заказами (OMS)

![Temporal Logo](https://temporal.io/logo.svg)

Демо-проект для изучения **Temporal** - платформы для оркестрации workflow, на примере системы управления заказами (Order Management System).

## 🎯 О проекте

Этот проект демонстрирует ключевые концепции Temporal на практическом примере обработки заказов в системе OMS. Вы увидите как Temporal обеспечивает надежное выполнение долгоживущих бизнес-процессов.

### Ключевые возможности Temporal:

- **Надежность** - Workflow продолжают выполнение после сбоев
- **Отслеживаемость** - Полная история выполнения каждого workflow
- **Гибкость** - Легко изменять бизнес-логику и добавлять новые этапы
- **Масштабируемость** - Горизонтальное масштабирование workers

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js** 18+ (для полной версии)
- **Docker** и **Docker Compose** (для полной версии)
- **Git** (для клонирования репозитория)

### Клонирование репозитория

```bash
# Клонируйте репозиторий
git clone <URL-репозитория>
cd SA_docs/temporal
```

**Или если проект уже локально:**
```bash
cd "C:\Users\user\Desktop\GIT\SA_docs\temporal"
```

## 🎮 Варианты запуска демо

### Вариант 1: PowerShell Demo (Самый простой)

**Не требует установки Node.js и Docker**

```bash
powershell -ExecutionPolicy Bypass -File "powershell-demo.ps1"
```

**Что вы увидите:**
- Успешную обработку заказа через все этапы
- Отмену заказа в процессе выполнения
- Activity функции в действии
- Объяснение концепций Temporal

### Вариант 2: Визуальное демо в браузере

**Наглядная визуализация архитектуры**

```bash
start visual-demo.html
```

**Особенности:**
- Визуализация взаимодействия компонентов
- Увеличенные таймеры для лучшего понимания (3-4 секунды на Activity)
- Анимированные статусы компонентов
- Цветная индикация workflow и activity

### Вариант 3: Полная версия с Temporal сервером

**Для продвинутого изучения**

```bash
# Установите зависимости
npm install

# Запустите Temporal сервер
docker-compose up -d

# В терминале 1 - запустите worker
npm run worker

# В терминале 2 - запустите клиент
npm run client
```

## 📁 Структура проекта

```
temporal/
├── 📄 README.md                 # Этот файл
├── 📄 INSTALL.md                # Подробная инструкция по установке
├── 📄 QUICK_RUN.md              # Быстрый старт
├── 📄 CONCEPTS.md               # Объяснение концепций Temporal
├── 📄 DEMO_COMPARISON.md        # Сравнение вариантов демо
│
├── 🚀 Запуск демо:
├── 📄 powershell-demo.ps1       # PowerShell версия (работает без Node.js)
├── 📄 visual-demo.html          # Визуальная версия с анимацией
├── 📄 browser-demo.html         # Базовая браузерная версия
├── 📄 simple-demo.js            # Node.js версия
│
├── 🔧 Полная реализация:
├── 📁 src/
│   ├── 📁 workflows/            # Workflow определения
│   │   └── order-workflow.ts    # Workflow процесса заказа
│   ├── 📁 activities/           # Activity функции
│   │   └── order-activities.ts  # Бизнес-процессы заказа
│   ├── 📁 client/               # Клиент для запуска workflow
│   │   └── order-client.ts      # Клиент заказа
│   └── 📁 worker/               # Worker для выполнения workflow
│       └── order-worker.ts      # Worker заказа
│
├── ⚙️ Конфигурация:
├── 📄 package.json              # Зависимости проекта
├── 📄 tsconfig.json             # TypeScript конфигурация
├── 📄 docker-compose.yml        # Docker для Temporal сервера
└── 📁 dynamicconfig/            # Динамическая конфигурация Temporal
    └── development.yaml
```

## 🏗️ Архитектура Temporal

### Компоненты системы:

1. **👤 Клиент** - Запускает workflow и взаимодействует с ними
2. **🔄 Workflow** - Долгоживущий бизнес-процесс (обработка заказа)
3. **🔧 Activity** - Отдельные бизнес-операции (проверка запасов, платеж и т.д.)
4. **⚙️ Worker** - Выполняет workflow и activity
5. **🏢 Temporal Server** - Оркестрирует выполнение workflow

### Workflow процесса заказа:

```
📝 СОЗДАНИЕ → 📦 ПРОВЕРКА ЗАПАСОВ → 💳 ОБРАБОТКА ПЛАТЕЖА 
    ↓
🎁 ПОДГОТОВКА → 🚚 ОТПРАВКА → ✅ ДОСТАВКА
```

### Activity функции:

- `checkInventory()` - Проверка наличия товаров
- `processPayment()` - Обработка платежа
- `prepareForShipment()` - Подготовка к отправке
- `shipOrder()` - Отправка заказа
- `notifyCustomer()` - Уведомление клиента
- `cancelOrder()` - Отмена заказа

## 💡 Концепции Temporal в демо

### Workflow
- **Долгоживущий процесс** обработки заказа
- **Сохраняет состояние** между перезапусками
- **Оркестрирует** выполнение activity функций
- **Обрабатывает сигналы** (например, отмену заказа)

### Activity
- **Отдельные бизнес-операции**
- **Могут содержать** любой код (API вызовы, работа с БД)
- **Имеют таймауты** и политики повторных попыток
- **Выполняются изолированно** от workflow

### Преимущества Temporal:
- **Надежность** - Workflow продолжают выполнение после сбоев серверов
- **Отслеживаемость** - Полная история выполнения в Temporal UI
- **Гибкость** - Легко добавлять новые этапы в бизнес-процесс
- **Масштабируемость** - Горизонтальное масштабирование workers

## 🎯 Что демонстрирует проект

### Бизнес-сценарии:

1. **Успешная обработка заказа** - Полный цикл от создания до доставки
2. **Отмена заказа** - Прерывание workflow по сигналу клиента
3. **Обработка ошибок** - Компенсирующие действия при сбоях

### Технические возможности:

- **Оркестрация** сложных бизнес-процессов
- **Обработка** долгоживущих операций
- **Управление** состоянием workflow
- **Взаимодействие** через сигналы

## 📚 Документация

### Для изучения:

1. **[CONCEPTS.md](CONCEPTS.md)** - Подробное объяснение концепций Temporal
2. **[INSTALL.md](INSTALL.md)** - Полная инструкция по установке и запуску
3. **[DEMO_COMPARISON.md](DEMO_COMPARISON.md)** - Сравнение вариантов демо

### Для запуска:

1. **[QUICK_RUN.md](QUICK_RUN.md)** - Быстрый старт с PowerShell
2. **[INSTALL.md](INSTALL.md)** - Полная установка с Temporal сервером

## 🔧 Разработка

### Запуск в режиме разработки:

```bash
# Установите зависимости
npm install

# Запустите Temporal сервер
docker-compose up -d

# Запустите worker в режиме разработки
npm run dev:worker

# Запустите клиент в режиме разработки
npm run dev:client
```

### Мониторинг:

- **Temporal UI**: http://localhost:8080
- **Temporal Server**: localhost:7233

## 🐛 Устранение проблем

### Распространенные проблемы:

1. **Node.js не установлен** - Используйте PowerShell или браузерную версию
2. **Docker не запущен** - Запустите Docker Desktop
3. **Порты заняты** - Измените порты в `docker-compose.yml`

### Решения:

```bash
# Проверьте что сервисы запущены
docker-compose ps

# Перезапустите Temporal сервер
docker-compose restart temporal

# Проверьте установку Node.js
node --version
npm --version
```

## 🤝 Вклад в проект

Этот проект создан для изучения Temporal. Вы можете:

1. **Добавлять новые activity** функции
2. **Расширять workflow** процесса
3. **Создавать новые** бизнес-сценарии
4. **Улучшать документацию** и примеры

## 🔗 Полезные ссылки

- [Официальная документация Temporal](https://docs.temporal.io/)
- [Temporal TypeScript SDK](https://typescript.temporal.io/)
- [Temporal UI Guide](https://docs.temporal.io/ui)
- [Temporal GitHub](https://github.com/temporalio)

---

**Начните изучение Temporal с запуска PowerShell демо и посмотрите как workflow оркестрирует процесс обработки заказа!**